---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: aegis-dashboard
  namespace: aegis
spec:
  parentRefs:
    - name: main-gateway
      namespace: default
  hostnames:
    - "aegis.web.id"
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: aegis-dashboard-svc
          port: 80
